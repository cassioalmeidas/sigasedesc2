  var modalContent = $('#modal-window');
  modalContent.html(
  `
    <%= j render 'shared/modal', 
          title: 'Detalhes do ativo', 
          body: render(@ativo),
          footer: @ativo.decorate.modal_links
    %>
  `
  );


  var modal = new bootstrap.Modal(modalContent, {
    keyboard: false
  })

  modal.show()
