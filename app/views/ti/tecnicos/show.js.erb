  var modalContent = $('#modal-window');
  modalContent.toggle();
  modalContent.html(
  `
    <%= j render 'shared/modal', 
          title: 'Detalhes do técnico', 
          body: @ti_tecnico.nome,
          footer: @ti_tecnico.decorate.modal_links
    %>
  `
  );


  var modal = new bootstrap.Modal(modalContent, {
    keyboard: false
  })

  modal.show()

