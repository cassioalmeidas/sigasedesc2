<div class="border-bottom mb-4 d-flex justify-content-between align-items-center">
  <h1>Serviços</h1>
  <%= link_to new_ti_servico_path, class: 'btn btn-success' do %>
    <%= icon 'fas', 'plus-square'  %> Cadastrar serviço
  <% end %>
</div>

<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Nome</th>
        <th colspan="3">Ações</th>
      </tr>
    </thead>

    <tbody>
      <% @ti_servicos.each do |ti_servico| %>
        <tr>
          <td><%= link_to ti_servico.nome, ti_servico, remote: true  %></td>
          <td>
            <%= icon 'fas', 'edit'  %>
            <%= link_to 'editar', edit_ti_servico_path(ti_servico) %>
            <%= icon 'fas', 'trash'  %> 
            <%= link_to 'remover', ti_servico, method: :delete, data: { confirm_swal: 'Você tem certeza?' }, class: 'text-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>