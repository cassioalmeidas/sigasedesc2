  var modalContent = $('#modal-window');
  modalContent.html(
  `
    <%= j render 'shared/modal', 
          title: 'Detalhes da unidade', 
          body: render(@unidade),
          footer: @unidade.decorate.modal_links
    %>
  `
  );


  var modal = new bootstrap.Modal(modalContent, {
    keyboard: false
  })

  modal.show()
